#include<stdio.h>

int main() {
    int nums[3];

    printf("Input 3 numbers\n");
    for (int i = 0; i < 3; i++){
        scanf("%d",&nums[i]);
    }

    // Null placeholders
    // Ran out of time in class
    for (int i = 0; NULL; NULL) {

    }
}